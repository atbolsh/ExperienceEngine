# Core dependencies
# LangChain 1.2.x: AgentExecutor / create_structured_chat_agent need langchain-classic.
# LangChain 0.2.x: same code paths use langchain.* (classic is optional there).
langchain>=0.2.0,<3.0.0
langchain-core>=0.2.0
langchain-text-splitters>=0.2.0
langchain-classic>=1.0.0
langchain-openai>=0.0.5
langchain-community>=0.0.20
langchain-huggingface>=0.1.0
openai>=1.12.0
python-dotenv>=1.0.0

# Vector store dependencies
faiss-cpu>=1.7.4
tiktoken>=0.5.2

# Local LLM dependencies (text + Qwen2-VL vision)
transformers>=4.45.0
torch>=2.0.0
accelerate>=0.25.0
flash-attn>=2.5.0
qwen-vl-utils>=0.0.8

# OpenCV (import cv2). Headless avoids GUI libs on servers; for desktop OpenCV windows use:
#   pip uninstall opencv-python-headless -y && pip install opencv-python
opencv-python-headless>=4.8.0

# Game environment (discreteEngine.py). Not optional when ACTIVE_ENVIRONMENT=game_environment.
# game_tools sets SDL_VIDEODRIVER=dummy for headless servers; pygame is still required for the module.
pygame>=2.5.0

# Additional utilities
pydantic>=2.0.0
# PyPI name (provides import sequential_thinking_tool); "sequential-thinking-tool" is not on PyPI
langchain-sequential-thinking-tool>=0.1.0

# Notebook UI (main.ipynb)
ipywidgets>=8.0.0


Goal: Face the nearest gold reliably.

Loop (Perception–Action):
1) Capture: use analyze_current_game_view with a focused question: "Is the nearest gold left, right, or centered relative to my current facing? How many small turns (~6°) would likely center it?"
2) Decision:
   - If answer says gold is to the right, turn_game_clockwise once; if left, turn_game_counterclockwise once.
   - If centered (within a few pixels or degrees), stop rotating.
3) After each single turn, call analyze_current_game_view again to confirm.
4) Avoid long action chains; limit to one turn per loop.

Notes:
- Use attached image for initial estimate but rely on live analyze_current_game_view during the loop.
- If multiple gold pieces are visible, define the target as the closest by Euclidean distance.
- Save occasional captures to working memory for debugging if alignment seems off.